# Bitcoin core RPC API Reference generator

## Dependencies
`bitcoind` and `bitcoin-cli` should be installed on the machine that wll generate the mdbook

## Usage
frome the repository root:
```shell 
cd src && ./generate.py && cd .. && mdbook build 
```